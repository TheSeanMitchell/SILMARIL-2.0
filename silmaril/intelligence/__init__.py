"""SILMARIL intelligence layer.

Deterministic, explainable analytics built on the day's debate + catalyst data.
NO LLMs, no tokens, no external calls — pure rule-based aggregation of data that
already lives on disk. The thesis (operator's): the written word is *predictive*;
numbers are reactive. This package surfaces the words/events that set up trades.
"""
