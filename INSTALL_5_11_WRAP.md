# INSTALL — 5.11 WRAP (drag-and-drop · built ON your July-13 4 PM live repo)

## Files (exact repo paths)
- silmaril/execution/{paper_sim.py, momentum_chain.py, invariants.py, confidence_engine.py,
  strategy_lab_abcd.py, conductor_report_card.py, bench_books.py, brain_wiring.py}
- scripts/{selftest_5_1.py, tag_suspect_history.py}
- docs/{index.html, silmaril_chart.js} · docs/data/PARAM_CATALOG.json (your live knobs + the
  new `compounder` knob merged — if you hand-tuned after 4 PM, merge that one key instead)
- .github/workflows/{integrity_backfill.yml, cleanup_5_11.yml}
- README.md · SILMARIL_MASTER_BRIEF.md · 5_11_WRAP_LEDGER.md · this file

## Order of operations tonight
1. Install (drag-drop all files) → commit.
2. Run **selftest** action → expect **29 pass** (your reported SyntaxError is fixed).
3. Optional but recommended history-honesty: run **integrity backfill** with confirm=TAG.
4. Run **cleanup_5_11** with confirm=SUNSET (attics superseded docs/workflows).
5. **Internal wipe** (your normal reset) → the recorder rebuilds a confirmed-clean tape in
   ~2h; Tuesday starts the clean week with everything armed.

## First cycles you'll see
Cards store (1050) + CONFIDENCE CARD block on every chart · per-industry labs with E⚡/F-vault
rows · "engine updated Xm ago" under the clock · OPEN-TRADE TRUTH under TODAY NET · QQQ in
NULLS · any sawtooth name shows ⚠ OSCILLATION-QUARANTINED instead of trading.

## Rollback
compounder.mode:"off" and vol_native.mode:"off" restore prior sizing/entries; everything else
is additive or a pure fix; your 4 PM backup is the twin of every replaced file.
