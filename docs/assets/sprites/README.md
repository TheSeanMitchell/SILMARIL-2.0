Drop-in sprite pack: place files named {index}.png (or .gif) here — index 0..319 matches the strategy
hash the dashboard uses. Any index you provide overrides the built-in animated critter for that slot
automatically; missing ones fall back. Nothing else to configure.
