# INSTALL — SILMARIL 7.0 (THE ACTIVATION) · drag-and-drop

## What this is
The final structural update. Everything after this is data flipping pre-registered knobs (Law 29) — no more code.

## Files (exact repo paths, all in the zip)
- `silmaril/execution/`: **geometry.py · sizer.py · calibration.py · edge_surface.py · retention.py · question_engine.py (NEW ×6)** · paper_sim.py · strategy_lab_abcd.py · master_account.py · champion_governance.py · brain_wiring.py · discovery.py · momentum_chain.py · venues.py · reconciliation.py · session_reconstruction.py · session_anatomy.py · conductor_report_card.py · confidence_engine.py · parameter_registry.py · ccxt_universe.py
- `silmaril/cli.py` (spine +6 stages) · `scripts/selftest_5_1.py` (battery **50**)
- `docs/index.html` (7.0 cockpit: Interrogator · Geometry · Governor · Calibration · Data Ledger · DSR · p\* chips)
- `docs/data/`: PARAM_CATALOG.json (your knobs + 7 new: geometry · maker_entries · sizer · calibration · edge_surface · retention · floor_confirm) · STORE_REGISTRY.json (7.0) · VENUES.json
- `.github/workflows/`: reset_internal_clean.yml (**+GENESIS mode**) · verify_install.yml (pins 7.0). Retired to `attic/workflows_retired_7_0/`: integrity_backfill · cleanup_5_11.
- Root: **LAWS.md · README.md · SILMARIL_7_0_MASTER_ROADMAP.md/.xml · INSTALL_7_0.md**

## Runbook (Sunday-ready)
1. Pause workflows → drag-drop everything → commit. (Your live data merges clean: state stores are untouched by the zip; new stores generate on first run.)
2. **Run one daily.** Geometry/Sizer/Calibration/Cells/Questions/DataLedger all generate; BRAIN shows 25/25.
3. **Selftest → expect 50 pass.**
4. **Wipe.** `standard` keeps learning (price tape, fingerprints); **`genesis`** resets learning too — your requested true-zero start with all 7.0 laws in force from trade one. Archives survive either way (Law 26/30).
5. Enable the 10-minute runner. **Freeze.** From here, only data flips knobs.

## Day-one expectations
Trade count will be LOW. That is the geometry gate + floor-confirm + one-factor law refusing unwinnable and unstable entries — the system finally saying no. Watch the Interrogator: its verdict should march from AWAY → HOLDING → TOWARD as evidence accumulates.

## Kills (pre-registered)
`geometry.mode:"off"` · `maker_entries.mode:"off"` · `sizer.mode:"off"` · `calibration.mode:"off"` · `edge_surface.mode:"observe"` (self-arms) · `floor_confirm.mode:"off"` · `retention.mode:"auto"` (leave on) · `master_brain.mode:"off"` · `discovery.mode:"off"`.
