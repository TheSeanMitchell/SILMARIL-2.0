# 09 · OPTIMIZATION ROADMAP — evidence-gated, in execution order

**Standing directive:** sniper improvements to win-rate and take-home; nothing outruns its evidence.

1. **Data harvest (now).** Let the healed lanes run. Gates: census fresh% climbing · every book
   ≥10 forward closed trades or census-excused · contracts+invariants green streak 30.
2. **DONE in 5.1B → now GRADE it.** The per-valuable shift sensor shipped as the MTF ladder and
   already routes exits/throttle/override/sizing. The next evidence step is reading its report
   card: harvest A/B (kill: median 24h saved ≤0 @60), sizing A/B (kill: ≤$0 @60), and the
   crash-avoidance Δ-vs-HODL line through the next red window.
3. **Conductor C1 → C2.** C1 gate opens at 300 scored decisions (live counter on the panel);
   after ≥150 matched cycles per policy, promote the shadow-best to ε=5% exploration on GEKKO ONLY
   (propensity-logged). C3 influence needs CI-positive uplift vs status quo.
4. **EXIT_LAB (F0→F1).** Trailing/ATR/partial-scale exits on the EXISTING MR entries — the
   hold-vs-harvest question the GEKKO bug accidentally posed, answered properly. Cheapest new
   evidence in the program (every open position feeds it).
5. **Parity labs (P6 generalized).** Stock 0.7–1.5% / metal 0.4–1.0% / energy 0.8–2.0% dip
   translations (entry≈1.5–2.5σ, target≈1–1.5σ, stop≈3σ of each market's own vol) so the quiet
   rooms trade or are census-excused.
6. **Heatshield sweep v2.** Replace the two-point comparison with a proper floor sweep store; the
   5.1 autotune resolver already consumes "measured winner", so it upgrades in place.
7. **Aggression ladder → Conductor action space** once ladder sleeves have n≥50 each.
8. **Decision-Replay UI** (deterministic core already permits byte-identical replay).
Every item: pre-registered death, Δ-vs-null scoring, contracts row, selftest where applicable.
