SUPERSEDED (2026-07-10 final audit): don't delete these by hand anymore.

Run the one-click workflow instead:
  Actions → "Cleanup 5.0 Final (one-time)" → Run workflow → type CLEAN

It attics the dead root code (cli.py, execution/, learning/ — reversible from
attic/) and deletes every orphaned data file the audit verified nothing writes
or reads (~25 MB, including the 21 MB _legacy_charts_disabled.json). The full
ledger of what it did prints in the run log. The earlier hand-list in this file
was partially wrong — three of its entries were still being written 3x/day by
the analytics suite; the audit doc has the details.
