# 5.1 REACTION LEDGER — every note → root cause → what shipped (2026-07-12, "5.1B")
Legend: **ROOT-CAUSED+FIXED** · **SHIPPED** new capability · **ANSWERED** with the honest reasoning · **VERIFIED** already working, receipts attached.

| # | Your note | Disposition |
|---|---|---|
| 1 | Header reads 3.0 again — "a workflow is undoing work" | **ROOT-CAUSED+FIXED** — two culprits: the `<h1>` used `&nbsp;` (my 5.1 replace missed it) AND runtime JS rewrote the header from `PROJECT_META.json`, whose version `verify_install.yml` literally asserted as `'3.0'`. Now: header is a CONSTANT, meta (pinned 5.1) is a display-only build stamp, verify asserts 5.1, and selftest **T9** trips if any of the three ever drifts again |
| 2 | Fallback depth still 0/N | **ROOT-CAUSED+FIXED** — health_lights guessed env names (`FINNHUB_KEY`); your workflows export `FINNHUB_API_KEY`-style names. Groups now use the exact names from daily.yml's env block (+ BIRDEYE/POLYGON/EIA added). First keyed cycle lights them |
| 3 | Opportunity Journal: TON +23598% ghosts; want WHY per miss; pair with Decision Trace; feed the Conductor | **SHIPPED** — journal now runs the sane-universe law (canonical · fresh ≤24h · one listing/base · \|move\|≤50%); every missed row carries the EXACT rule from OPPORTUNITY_AUDIT ("insufficient_drop: moved −1.2%, rule needs ≤−3%"), open names read "CAPITAL COMMITTED", a **CAPITAL STUCK** strip lists underwater ≥72h wagers, and the panel states its Decision-Trace pairing. The Conductor's report card consumes the stuck list |
| 4 | Stuck capital: pull out at 24/36/48/72h? Does this force market orders / higher fees? Realistic? | **SHIPPED+ANSWERED** — `stale_capital` knob: past **36h** with head above water → `FEE_CLEAR_TIME` exit banks it and frees the capital (never forces a loss; underwater just gets flagged at 72h). Fee honesty: these are **limit-class exits at the live print, same maker cost model as every TAKE — no fee-bracket change**. Realism: resting/marketable limit orders are exactly how a live venue would do this. And it's an **A/B**: every event logs to `REGIME_EXIT_AB.jsonl`; the report card grades saved% at +6h/+24h with a pre-registered kill (median ≤0 after 60 → turn it off) |
| 5 | "You didn't reply to our news concerns" | **ANSWERED (explicitly this time)** — prior drop fixed direct article links + one dead stream; this drop executes the rest: feeds #7–9 **removed** (six remain), streams are click-to-load (nothing stale autoplays), mobile shows ONE video with ◀ ▶ paging, desktop keeps the wall + fullscreen. News influence on trading remains OBSERVE-gated behind the 90-day trial — headlines inform the research layer, never a trade, until the hit-rate beats a coin flip |
| 6 | Triple timestamps everywhere (Vegas · market · universal) | **SHIPPED** — header clock shows **Vegas · NY · UTC**; every rendered timestamp site converted to the triple form via `T3()/T3t()` helpers (9 call sites swept). No lone clocks remain |
| 7 | Wipe workflow 5.1-ready; backfill fingerprints for the ENTIRE universe, all industries | **VERIFIED+FIXED** — reset preserves every 5.1B long-memory store (list extended: `REGIME_EXIT_AB`, `CONDUCTOR_REPORT_CARD`); backfill + pulse feed fingerprints from ALL classes. The real blocker was the fitter's universe: first-500-in-dict-order let crypto flood the cap — **class quotas now admit metals+energy FIRST**, so **GOLD gets its custom fit on the very next cycles** and the metal book trades its own rhythm when markets open |
| 8 | Fingerprints for every valuable; port to new industries; GOLD NOW | **SHIPPED** — same fix as #7; coverage grows to all industries under the 500 ceiling (metal ∞ · energy ∞ · stock 160 · crypto fills). The fitting engine was never crypto-gated — its diet was |
| 9 | C0 268/300 — "starts changing behaviors at 300?" | **ANSWERED (and printed on the SPINE)** — **No.** 300 opens the C1 **ANALYSIS** gate: shadow-scoring policies against the log, zero trades. Behavior changes only at C2 — a tiny ε on GEKKO alone — and only after C1 shows positive uplift. Never automatic |
| 10 | SPINE: clearer, expanded, more informing language | **SHIPPED** — new rows: **MTF LADDER** (per-book confluence + fast-red state), **CONDUCTOR C1** (scored count + per-policy bps), **REPORT CARD** (harvest A/B · sizing A/B · stuck $ · realized $), C0 clarifier, plain-language tails throughout |
| 11 | Repo ~204.9MB — are we throwing away fingerprint/prediction fuel? | **ANSWERED** — pruning is knob-governed (`prune`: ~2,000 intraday points per symbol ≈ two weeks at pulse cadence, ledger caps) and **never touches long-memory** (evolution ledger, research OS, census roster, conductor ledgers, A/B ledgers — wipe-proof AND prune-proof). Fingerprints need recent rhythm, not infinite history; raise `intraday_keep_per_symbol` any time if you want deeper fits — it's one knob |
| 12 | Scorecard shows "undefined" everywhere | **ROOT-CAUSED+FIXED** — renderer expected the pre-5.1 object shape; it now renders the evidence shape (name · letter grade · score · **printed formula** · evidence line). Selftest **T10** enforces the store⇄renderer contract forever |
| 13 | Flat $1000 every trade — the system doesn't know a good bet from a bad one; the 95%-confidence vision | **SHIPPED** — `conviction_sizing`: wager = base × (0.5–2.5×) from **0.45·fingerprint bounce-reliability + 0.35·this book's own win-rate on the symbol + 0.20·MTF confluence**, clamped 5–25% of equity. Every sized trade logs its flat-base twin → **SIZING A/B** on the report card (kill: ≤$0 extra after 60 closed). Your 95%→$5,500 Master vision is exactly this curve at Master scale — it inherits the mechanism the day the Master earns trading rights |
| 14 | THE CRASH LESSON: multi-timeframe regime 15m→1mo per industry AND valuable; confidence stacking; symbol beats industry; sell out fee-clear on flips; open-order caps; be first to react | **SHIPPED — the centerpiece.** `mtf_regime.py` votes **12 timeframes (15m→30d) per industry and per valuable** with a printed confluence formula; **fast_red** (15m·30m·1h unanimous) is the early tripwire. On fast_red: `REGIME_FLIP_HARVEST` banks every net-green position (fee-clear, limit-class), `regime_throttle` caps new entries to 1/cycle, and a valuable with confluence ≥5 may still trade through the industry red (max 2/cycle) — your bitcoin-in-a-bad-regime case. On July-11's slide this stack would have harvested the morning's green and stopped marching capital downhill. Every piece is knob-off-able and A/B-graded |
| 15 | Conductor honesty & integrity system — judge it harshly; true scientific experiment | **SHIPPED** — `CONDUCTOR_REPORT_CARD.json`: **harvest A/B** (saved% at +6h/+24h vs the exit), **sizing A/B** (extra $ vs flat twins), **stuck-capital** dollars, **crash-avoidance** (Δ-vs-HODL through red windows), realized-profit tally — every grade a formula, every behavior a pre-registered kill criterion. The card, not enthusiasm, decides what stays |
| 16 | Profits tied up aren't profits — SGOV harvest? Track without fees/tax thresholds? | **ANSWERED+SHIPPED** — the honest answer: inside a paper sim, a **ledger** is the fee-free harvest. The report card now carries `cumulative_realized_usd_all_books` (realized only, fees inside) — your "do we actually have profits" number, no sweep trades, no extra fees, no tax events manufactured. An SGOV-style sweep only makes sense with live money (it's a real yield on real cash); the day the Master goes live, the sweep becomes one knob and this same line prices it |
| 17 | Front page order: Golden → 10k books → LIVE POSITIONS → closed trades → take-home; free aesthetic reign | **SHIPPED** — boot reorder now walks exactly that sequence and adds a compact **TODAY NET — kept after fees** line beneath the trades (full table stays on Forensics). Deeper aesthetic overhaul noted as accepted freedom — deferred behind data-harvest priorities, on the roadmap |
| 18 | Broker LEGACY yellow is annoying — we don't use it | **SHIPPED** — LEGACY rows render **GRAY “retired — unused Alpaca legacy, not a fault”**; it can never look broken again |
| 19 | Workflow folder cleanup + TOC + correct timers + external-cron with eternal tokens, "just do that now" | **SHIPPED** — `.github/WORKFLOWS_README.md` documents **all 14 files** (purpose · trigger · timing · writes · edit-safety · peak-hours rule); every scheduled job now carries the `EXTERNAL_CRON_ONLY` guard; `CRON_SETUP.md` gives the no-expiration fine-grained PAT recipe + the exact dispatch endpoint and schedule per lane. Set one repo variable and internal timers become no-ops — reversible in one click |
| 20 | Docs must reflect the FINAL 5.1 form | **SHIPPED** — README checklist + DOCS_5_1 updated for every 5.1B system (this file included); verify_install audits all new markers; selftest is now a **14-tripwire** battery |
