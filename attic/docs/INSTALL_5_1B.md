# INSTALL — 5.1B REACTION COMPLETE (drag-and-drop; additive; NO wipe required)

## ORDER
1. `silmaril/execution/` — **paper_sim.py** · **opportunity_journal.py** · **health_lights.py** · **mtf_regime.py** (new) · **conductor_report_card.py** (new)
2. `silmaril/cli.py` · `scripts/selftest_5_1.py` · `scripts/reset_internal_clean.py`
3. `docs/index.html` · `docs/manifest.webmanifest` (new) · `docs/sw.js` (new) · `docs/data/PROJECT_META.json` · `docs/data/PARAM_CATALOG.json`
   ⚠ the catalog = your July-11 6PM knobs + four new objects (`regime_exit · stale_capital · conviction_sizing · regime_throttle`). If you've hand-tuned since, add ONLY those four objects instead of overwriting
4. `.github/workflows/` — daily · hourly · analytics · backfill_universe · weekly_backup · selftest · **verify_install** (now asserts 5.1) · plus `.github/WORKFLOWS_README.md`
5. Root — `CRON_SETUP.md` · `5_1_REACTION_LEDGER.md` · this file · updated `README.md` + `DOCS_5_1/`

## FIRST CYCLES — what you'll see
- Header **SILMARIL 5.1** with a tiny `build 5.1` stamp — and it can never revert (T9 guards all three sources)
- Fallback depth turns real on the first daily/hourly (real env names); LEGACY shows gray "retired"
- `MTF_REGIME.json` appears (ladder renders under LIVE REGIME); the first fast-red day engages harvest + throttle and starts the A/B ledger
- `CONDUCTOR_REPORT_CARD.json` appears on the SPINE; grades fill as events age past 6h/24h
- Scorecard shows names + letter grades + formulas; Opportunity Journal shows WHY per miss + CAPITAL STUCK
- GOLD (and every metal/energy name) enters the fingerprint fit within the first cycles — watch FINGERPRINTS coverage
- Triple clocks everywhere; news = six click-to-load streams, one-at-a-time on mobile; ⬇ Install app button under the Binance card
- Run **selftest** from Actions: expect **14 pass**. Run **verify-install**: every marker OK, version 5.1
- Optional, when ready: follow `CRON_SETUP.md`, set `EXTERNAL_CRON_ONLY=true` — internal timers go quiet

## ROLLBACK
Your 6PM backup is the twin of every changed file; the four knobs are additive objects; the cron switch is one repo variable.
