# INSTALL — 5.11 ACTIVATION (drag-and-drop · additive · NO wipe needed — built ON your 11 AM live repo)

## FILES (replace/add at these exact paths)
1. `silmaril/execution/paper_sim.py` — vol-native entries + position caps + fp150
2. `silmaril/execution/gate_evidence.py` — Dr. Strange career → gate
3. `silmaril/execution/brain_wiring.py` — **NEW**, the nothing-is-decoration engine
4. `silmaril/cli.py` — brain_wiring on the spine
5. `scripts/selftest_5_1.py` — battery → 26 tripwires
6. `docs/index.html` — BRAIN tab (7 panels) + cR fix + NaN-proof readiness
7. `docs/data/PARAM_CATALOG.json` — YOUR live 11 AM knobs + `vol_native` + `position_caps` +
   metal/energy DOWNTREND profiles (⚠ if you hand-tuned after 11 AM, add those four objects to
   your file instead of overwriting)
8. `DOCS_5_1/13_BRAIN.md` · `5_11_ACTIVATION_LEDGER.md` · this file

## FIRST CYCLES — what you'll see
- A 🧠 BRAIN tab between COMMAND and STRATEGY; `BRAIN_WIRING.json` appears on the first pulse
- SIGNAL LEDGER shows **14/14 wired** with shift arrows; dossiers fill with your open positions;
  the eyeshot grid shows every held name with entry/target/stop lines
- The dr_strange gate row starts counting its real resolved picks + hit-rate
- Vol-native action notes appear as quiet-market names qualify on their OWN bars; metal/energy
  arm the moment their sessions print real moves (see ledger — the weekend "freeze" was the
  system refusing stale prints, correctly)
- Run **selftest** from Actions: expect **26 pass**

## ROLLBACK
`vol_native.mode:"off"` and deleting `position_caps` restore prior behavior instantly; every
other change is additive or a pure fix. Your 11 AM backup is the twin of every replaced file.
